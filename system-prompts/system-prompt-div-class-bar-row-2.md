# System Prompt: div-class-bar-row-2

- Source: inline

## Summary

HTML structure for a bar with labels and values.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |
| `EXPR_2` | None | None |

# Raw Prompt Text
<div class="bar-row">
        <div class="bar-label">${EXPR_1}<${PATH}>
        <div class="bar-track"><div class="bar-fill" style="width:stable%;background:#6366f1"><${PATH}><${PATH}>
        <div class="bar-value">${EXPR_2}<${PATH}>
      <${PATH}>
