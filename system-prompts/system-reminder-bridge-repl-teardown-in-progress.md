# System Prompt: bridge-repl-teardown-in-progress

- Source: inline

## Summary

Teardown process is already underway.

# Raw Prompt Text
[bridge:repl] Teardown already in progress, skipping duplicate call env=${NUM} session=${EXPR_1}
