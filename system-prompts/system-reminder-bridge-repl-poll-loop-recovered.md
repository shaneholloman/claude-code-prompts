# System Prompt: bridge-repl-poll-loop-recovered

- Source: inline

## Summary

Poll loop recovered during stopWork await.

# Raw Prompt Text
[bridge:repl] Poll loop recovered during stopWork await — deferring to it
