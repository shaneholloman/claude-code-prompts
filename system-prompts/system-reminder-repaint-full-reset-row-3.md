# System Prompt: repaint-full-reset-row-3

- Source: inline

## Summary

Instructions for a full repaint reset.

# Raw Prompt Text
[REPAINT] full reset · ${EXPR_1} · row ${EXPR_2}
  prev: "${EXPR_3}"
  next: "${EXPR_4}"
  culprit: ${EXPR_5} < ${EXPR_6} < ${EXPR_7} < ${EXPR_8} < ${EXPR_9} < ${EXPR_10} < ${EXPR_11} < ${EXPR_12} < ${EXPR_13} < ${EXPR_14} < ${EXPR_15}
