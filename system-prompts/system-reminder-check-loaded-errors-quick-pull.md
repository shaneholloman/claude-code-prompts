# System Prompt: check-loaded-errors-quick-pull

- Source: inline

## Summary

Check if PID has loaded errors for quick pull.

# Raw Prompt Text
${URL}${EXPR_1}${PATH} (PID ${EXPR_2})...${EXPR_3} loaded with errors?quick_pull=${NUM}&title=${EXPR_4}&body=${EXPR_5}
