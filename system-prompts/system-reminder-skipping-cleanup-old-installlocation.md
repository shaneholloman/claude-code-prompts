# System Prompt: skipping-cleanup-old-installlocation

- Source: inline

## Summary

Skipping cleanup due to corrupted path.

# Raw Prompt Text
Skipping cleanup of old installLocation (${EXPR_1}) — outside @anthropic-ai${PATH} The path is corrupted; leaving it alone and overwriting the config entry.
