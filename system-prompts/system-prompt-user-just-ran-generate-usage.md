# System Prompt: 61a1f369


## Summary

The user just ran … to generate a usage report analyzing their Claude Code sessions.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |
| `EXPR_2` | None | None |
| `EXPR_3` | None | None |
| `EXPR_4` | None | None |
| `EXPR_5` | unknown | None |
| `EXPR_6` | None | None |
| `EXPR_7` | None | None |

# Raw Prompt Text
The user just ran ${PATH} to generate a usage report analyzing their Claude Code sessions.

Here is the full insights data:
${EXPR_1}

Report URL:  · ${EXPR_2}/${EXPR_3} to scroll
HTML file: ${NUM}
Facets directory: ${EXPR_4}

Here is what the user sees:
${EXPR_5: 'unknown'}

Now output the following message exactly:

<message>
Your shareable insights report is ready:
 · ${EXPR_6}/${EXPR_7} to scroll@anthropic-ai${PATH}

Want to dig into any section or try one of the suggestions?
<${PATH}>
