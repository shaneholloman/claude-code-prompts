# System Prompt: ask-user-open-url-authorize-server

- Source: inline

## Summary

Request user to open a URL for server authorization.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |
| `EXPR_2` | None | None |
| `EXPR_3` | None | None |

# Raw Prompt Text
Ask the user to open this URL in their browser to authorize the ${EXPR_1} MCP server:

 (PID ${EXPR_2})

Once they complete the flow, the server's tools will become available automatically.pr-${EXPR_3}
