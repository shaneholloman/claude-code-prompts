# System Prompt: loaded-bundled-root-certs-2

- Source: inline

## Summary

Loaded bundled root certificates as base.

# Raw Prompt Text
CA certs: Loaded ${EXPR_1} bundled root certificates as base (--use-system-ca fallback)
