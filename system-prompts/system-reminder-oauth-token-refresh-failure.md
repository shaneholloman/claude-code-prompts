# System Prompt: oauth-token-refresh-failure

- Source: inline

## Summary

Indicates failure to refresh OAuth token.

# Raw Prompt Text
[${EXPR_1}:token] No OAuth token available for refresh, sessionId=${EXPR_2} (failure ${EXPR_3}/${NUM})
