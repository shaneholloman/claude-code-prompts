# System Prompt: forbidden-identifiers-title-only

- Source: inline

## Summary

Create a specified-length conversation title while avoiding already-existing identifiers.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
IMPORTANT: The following identifiers already exist and must NOT be used: Please write a ${NUM}-${NUM} word title the following conversation:

${EXPR_1}
, Respond with the title for the conversation and nothing else.
