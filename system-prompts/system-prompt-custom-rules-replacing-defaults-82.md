# System Prompt: custom-rules-replacing-defaults-82

- Source: inline

## Summary

Custom rules replace default settings.

# Raw Prompt Text
stable

npm view ${EXPR_1}@${EXPR_2} version

## allow (custom rules replacing defaults)
Custom:
${EXPR_3}

Defaults being replaced:
${EXPR_4}

## soft_deny (custom rules replacing defaults)
Custom:
${EXPR_5}

Defaults being replaced:
${EXPR_6}

## environment (custom rules replacing defaults)
Custom:
${EXPR_7}

Defaults being replaced:
${EXPR_8}



GitHub not connected for ${EXPR_9}/${EXPR_10} — run ${PATH} to sync your GitHub credentials, or install the Claude GitHub App at ${URL}
