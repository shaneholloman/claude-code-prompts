# System Prompt: create-statusline-setup-task


## Summary

Creates a task that invokes the statusline setup subagent.

# Raw Prompt Text
Create a Task with subagent_type "statusline-setup" and the prompt "Configure my statusLine from my shell PS1 configuration"
