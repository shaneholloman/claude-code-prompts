# System Prompt: send-message-usage

## Summary

Instructions for sending a message to an agent.

# Raw Prompt Text
agentId: ${EXPR_1} (use SendMessage with to: '${EXPR_2}' to continue this agent)${EXPR_3}
<usage>total_tokens: ${EXPR_4}
tool_uses: ${EXPR_5}
duration_ms: ${EXPR_6}<${PATH}>
