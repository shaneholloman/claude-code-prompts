# System Prompt: bridge-sdk-state-change

- Source: inline

## Summary

Indicates a state change in the bridge SDK.

# Raw Prompt Text
[bridge:sdk] State change: ${EXPR_1}-${EXPR_2}${EXPR_3} — ${EXPR_4}
