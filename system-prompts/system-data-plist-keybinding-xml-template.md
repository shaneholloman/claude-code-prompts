# System Data Block: plist-keybinding-xml-template

- Source: inline

## Summary

XML property-list snippet for a keybinding entry with numeric fields.

# Raw Prompt Text
<dict>
      <key>Text<${PATH}>
      <string>\n<${PATH}>
      <key>Action<${PATH}>
      <integer>${NUM}<${PATH}>
      <key>Version<${PATH}>
      <integer>${NUM}<${PATH}>
      <key>Keycode<${PATH}>
      <integer>${NUM}<${PATH}>
      <key>Modifiers<${PATH}>
      <integer>${NUM}<${PATH}>
    <${PATH}>
