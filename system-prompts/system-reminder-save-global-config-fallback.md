# System Prompt: save-global-config-fallback

- Source: inline

## Summary

Refusing to write due to missing auth in config.

# Raw Prompt Text
saveGlobalConfig fallback: re-read config is missing auth that cache has; refusing to write. See GH #${NUM}.
