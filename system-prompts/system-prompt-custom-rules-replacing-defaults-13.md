# System Prompt: custom-rules-replacing-defaults-13

- Source: inline

## Summary

Defines custom rules to replace default settings.

# Raw Prompt Text
${EXPR_1}Sub values: npm view ${EXPR_2}@${EXPR_3} version## allow (custom rules replacing defaults)
Custom:
${EXPR_4}

Defaults being replaced:
${EXPR_5}

## soft_deny (custom rules replacing defaults)
Custom:
${EXPR_6}

Defaults being replaced:
${EXPR_7}

## environment (custom rules replacing defaults)
Custom:
${EXPR_8}

Defaults being replaced:
${EXPR_9}
