# System Reminder: ssh-clone-failed-fallback-https

- Source: inline

## Summary

SSH clone failed for the target despite configuration; falling back to HTTPS.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
SSH clone failed for ${EXPR_1} despite SSH being configured, falling back to HTTPS
