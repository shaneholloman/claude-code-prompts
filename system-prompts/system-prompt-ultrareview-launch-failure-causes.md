# System Prompt: ultrareview-launch-failure-causes

## Summary

Check for git repo, login status, and CCR connectivity.

# Raw Prompt Text
Ultrareview failed to launch. Common causes: not a git repo, not logged in (run `${PATH}`), or CCR connectivity. Tell the user what to check.
