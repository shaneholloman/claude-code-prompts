# System Prompt: mcp-server-skipping-customization

- Source: inline

## Summary

Skipping MCP servers due to strict plugin-only settings.

# Raw Prompt Text
[Agent: ${EXPR_1}] Skipping MCP servers: strictPluginOnlyCustomization locks MCP to plugin-only (agent source: ${EXPR_2})
