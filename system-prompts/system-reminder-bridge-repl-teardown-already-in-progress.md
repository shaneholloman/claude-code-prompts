# System Reminder: bridge-repl-teardown-already-in-progress

- Source: inline

## Summary

Teardown already in progress, skipping duplicate call.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | 0 | None |

# Raw Prompt Text
[bridge:repl] Teardown already in progress, skipping duplicate call env=Found ${EXPR_1: 0} MCP servers in Claude Desktop. session=stream-json
