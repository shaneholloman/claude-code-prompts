# System Prompt: oauth-token-refresh-error

- Source: inline

## Summary

No OAuth token available for refresh.

# Raw Prompt Text
[${EXPR_1}:token] No OAuth token available for refresh, sessionId=${EXPR_2} loaded with errors (failure ${EXPR_3} failed to load/${NUM})
