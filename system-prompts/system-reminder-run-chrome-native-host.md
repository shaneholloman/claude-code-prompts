# System Prompt: run-chrome-native-host

- Source: inline

## Summary

Invoke a command with two arguments and the chrome native host flag.

# Raw Prompt Text
"${EXPR_1}" "${EXPR_2}" --chrome-native-host
