# System Prompt: bridge-token-reconnect-failed

- Source: inline

## Summary

Indicates a failed session reconnection attempt.

# Raw Prompt Text
[bridge:token] reconnectSession(${EXPR_1}) failed: ${EXPR_2}
