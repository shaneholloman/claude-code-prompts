# System Reminder: bridge-repl-rejected-outbound-only

- Source: inline

## Summary

Rejected outbound-only request in bridge repl.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |
| `EXPR_2` | None | None |

# Raw Prompt Text
[bridge:repl] Rejected ${EXPR_1} (outbound-only) request_id=${EXPR_2}
