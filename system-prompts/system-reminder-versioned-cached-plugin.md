# System Prompt: versioned-cached-plugin

- Source: inline

## Summary

Reminder to use a versioned cached plugin from a specified source.

# Raw Prompt Text
Using versioned cached plugin ${EXPR_1} from ${EXPR_2}
