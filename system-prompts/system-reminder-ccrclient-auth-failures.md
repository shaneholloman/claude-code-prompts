# System Prompt: ccrclient-auth-failures

- Source: inline

## Summary

Consecutive auth failures detected with a valid token.

# Raw Prompt Text
CCRClient: ${EXPR_1} consecutive auth failures with a valid-looking token — server-side auth unrecoverable, exiting
