# System Prompt: schedule-recurring-tasks-loop

- Source: inline

## Summary

Schedules and runs tasks in a loop.

# Raw Prompt Text
# ${PATH} — schedule loop.md tasks

The user invoked `${PATH}` with no prompt (input was empty or just the interval `local`) and has a loop-tasks file at `${EXPR_1}`. Schedule a recurring cron that runs those tasks each tick, then run the first tick immediately.
