# System Prompt: skip-duplicate-inode-file

- Source: inline

## Summary

Skip duplicate file because the same inode was already loaded elsewhere.

# Raw Prompt Text
Skipping duplicate file '${EXPR_1}' from ${EXPR_2} (same inode already loaded from ${EXPR_3}@${EXPR_4})
