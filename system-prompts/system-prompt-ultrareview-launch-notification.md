# System Prompt: ultrareview-launch-notification

## Summary

Acknowledge the launch of Ultrareview with errors.

# Raw Prompt Text
Ultrareview launched for ${EXPR_1} loaded with errors (~${NUM}–${NUM} min, runs in the cloud). Track: ${EXPR_2}${EXPR_3} Findings arrive via task-notification. Briefly acknowledge the launch to the user without repeating the target or URL — both are already visible in the tool output above.
