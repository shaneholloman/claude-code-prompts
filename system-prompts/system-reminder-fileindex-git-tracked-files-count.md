# System Prompt: fileindex-git-tracked-files-count

- Source: inline

## Summary

Log tracked file count and git ls-files timing with process identifier.

# Raw Prompt Text
[FileIndex] git ls-files: ${EXPR_1} tracked files in  (PID ${EXPR_2})ms
