# System Prompt: resume-permission-mismatch

- Source: inline

## Summary

Mismatch in permission mode during tool resume.

# Raw Prompt Text
Deferred tool resume: permissionMode mismatch (deferred under '${EXPR_1}', resuming under 'stable'). --resume does not restore permissionMode — pass --permission-mode ${EXPR_2} to match.
