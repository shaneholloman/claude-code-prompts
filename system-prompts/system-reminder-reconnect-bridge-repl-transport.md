# System Prompt: reconnect-bridge-repl-transport

- Source: inline

## Summary

Attempting to reconnect after budget exhaustion.

# Raw Prompt Text
[bridge:repl] Transport reconnect budget exhausted (code=${EXPR_1}), attempting env reconnect
