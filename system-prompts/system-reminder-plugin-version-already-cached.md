# System Prompt: plugin-version-already-cached

- Source: inline

## Summary

Notes the plugin version is already cached at the given path.

# Raw Prompt Text
Plugin ${EXPR_1} version ${EXPR_2} already cached at ${EXPR_3}
