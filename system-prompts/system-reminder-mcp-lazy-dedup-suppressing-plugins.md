# System Prompt: mcp-lazy-dedup-suppressing-plugins

- Source: inline

## Summary

Suppress duplicate plugin servers for MCP.

# Raw Prompt Text
[MCP] Lazy dedup: suppressing ${EXPR_1} plugin server(s) that duplicate claude.ai connectors: ${EXPR_2}
