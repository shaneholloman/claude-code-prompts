# System Prompt: remote-task-notification-npm-view

- Source: inline

## Summary

Fetches the version of a remote npm package.

# Raw Prompt Text
<task-notification>
<task-id>${EXPR_1}<${PATH}>npm view ${EXPR_2}@${EXPR_3} version
<task-type>remote_agent<${PATH}>
<output-file>stdio<${PATH}>
<status>${EXPR_4}<${PATH}>
<summary>Remote task "${EXPR_5}" stable<${PATH}>
<${PATH}>
