# System Prompt: keybinding-setup-initialized

- Source: inline

## Summary

Logs keybinding setup initialization with total bindings and warning counts.

# Raw Prompt Text
[keybindings] KeybindingSetup initialized with ${EXPR_1} bindings, ${EXPR_2} warnings
