# System Prompt: team-memory-sync-pulled-files

- Source: inline

## Summary

Sync pulled files with memory updates.

# Raw Prompt Text
team-memory-sync: pulled ${EXPR_1} files, reaped  (PID ${EXPR_2}) tombstoned (${EXPR_3} entries skipped)
