# System Prompt: scheduled-token-refresh-errors

- Source: inline

## Summary

Scheduled token refresh encountered errors.

# Raw Prompt Text
[${EXPR_1}:token] Scheduled token refresh for sessionId=${EXPR_2} loaded with errors in ${EXPR_3}s (expires=unknown, buffer=${EXPR_4}s)
