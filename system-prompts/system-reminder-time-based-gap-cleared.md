# System Prompt: time-based-gap-cleared

- Source: inline

## Summary

Clears tool results based on time gaps.

# Raw Prompt Text
[TIME-BASED MC] gap ${EXPR_1}min > ${EXPR_2}min, cleared ${EXPR_3} tool results (~@anthropic-ai${PATH} tokens), kept last ${EXPR_4}
