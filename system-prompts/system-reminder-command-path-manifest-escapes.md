# System Prompt: command-path-manifest-escapes

- Source: inline

## Summary

Command path specified in manifest escapes plugin directory.

# Raw Prompt Text
Command path ${EXPR_1} specified in manifest but escapes plugin directory for ${EXPR_2}
