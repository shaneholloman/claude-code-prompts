# System Prompt: div-class-bar-row

- Source: inline

## Summary

HTML structure for a bar with labels and values.

# Raw Prompt Text
<div class="bar-row">
        <div class="bar-label">${EXPR_1}<${PATH}>
        <div class="bar-track"><div class="bar-fill" style="width:${EXPR_2}%;background:${EXPR_3}"><${PATH}><${PATH}>
        <div class="bar-value">stdio<${PATH}>
      <${PATH}>
