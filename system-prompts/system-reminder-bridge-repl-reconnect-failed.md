# System Prompt: bridge-repl-reconnect-failed

- Source: inline

## Summary

Reconnect session failed, falling to fresh session.

# Raw Prompt Text
[bridge:repl] reconnectSession failed: ${EXPR_1} — falling through to fresh session
