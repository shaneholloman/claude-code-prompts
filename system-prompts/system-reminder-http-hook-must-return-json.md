# System Reminder: http-hook-must-return-json

- Source: inline

## Summary

HTTP hook requires a JSON response.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
HTTP hook must return JSON, but got non-JSON response body: ${EXPR_1}
