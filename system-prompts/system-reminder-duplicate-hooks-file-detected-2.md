# System Prompt: duplicate-hooks-file-detected-2

- Source: inline

## Summary

Hooks file resolves to an already-loaded file.

# Raw Prompt Text
Duplicate hooks file detected: ${EXPR_1} resolves to already-loaded file null. The standard hooks${PATH} is loaded automatically, so manifest.hooks should only reference additional hook files.
