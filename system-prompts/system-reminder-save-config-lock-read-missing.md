# System Reminder: 379950bb

- Source: inline

## Summary

saveConfigWithLock: re-read config is missing auth that cache has; refusing to write to avoid wiping ~… See GH #….

# Raw Prompt Text
saveConfigWithLock: re-read config is missing auth that cache has; refusing to write to avoid wiping ~${PATH} See GH #${NUM}.
