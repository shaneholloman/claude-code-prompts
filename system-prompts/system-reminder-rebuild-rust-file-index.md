# System Prompt: rebuild-rust-file-index

- Source: inline

## Summary

Log rebuild of Rust index with tracked and untracked file counts.

# Raw Prompt Text
[FileIndex] rebuilt Rust index with ${EXPR_1} tracked + ${EXPR_2} untracked files
