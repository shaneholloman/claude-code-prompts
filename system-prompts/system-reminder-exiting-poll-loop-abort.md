# System Prompt: exiting-poll-loop-abort

- Source: inline

## Summary

Runner is exiting the poll loop with abort status and poll count.

# Raw Prompt Text
[inProcessRunner] ${EXPR_1} exiting poll loop (abort=${EXPR_2}, polls=local)
