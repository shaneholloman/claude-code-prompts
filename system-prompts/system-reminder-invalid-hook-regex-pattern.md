# System Prompt: invalid-hook-regex-pattern

- Source: inline

## Summary

Reports an invalid regular expression pattern used in a hook matcher.

# Raw Prompt Text
Invalid regex pattern in hook matcher: ${EXPR_1}
