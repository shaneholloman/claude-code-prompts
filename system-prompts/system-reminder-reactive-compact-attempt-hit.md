# System Prompt: reactive-compact-attempt-hit

- Source: inline

## Summary

Handles reactive compact attempts with local preservation.

# Raw Prompt Text
Reactive compact: attempt ${EXPR_1} hit prompt-too-long (gap=${EXPR_2} → ${EXPR_3} step ${EXPR_4}), next preserves local${PATH}
