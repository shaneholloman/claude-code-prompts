# System Prompt: repaint-full-reset-row-2

- Source: inline

## Summary

Full reset and repaint for specified row.

# Raw Prompt Text
[REPAINT] full reset · ${EXPR_1} · row ${EXPR_2}
  prev: "${EXPR_3}"
  next: "${EXPR_4}"
  culprit: verb < lstinline < ${EXPR_5} < ${EXPR_6}
