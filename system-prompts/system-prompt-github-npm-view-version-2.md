# System Prompt: github-npm-view-version-2

- Source: inline

## Summary

Check the stable version of a package on npm.

# Raw Prompt Text
stable

npm view ${EXPR_1}@${EXPR_2} version

stdio

${EXPR_3}

waiting_for_agents

GitHub not connected for ${EXPR_4}/${EXPR_5} — run ${PATH} to sync your GitHub credentials, or install the Claude GitHub App at ${URL}
