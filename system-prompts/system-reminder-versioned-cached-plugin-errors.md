# System Prompt: versioned-cached-plugin-errors

- Source: inline

## Summary

Using a cached plugin with loading errors.

# Raw Prompt Text
Using versioned cached plugin ${EXPR_1} from ${EXPR_2} loaded with errors
