# System Prompt: skip-circular-symlink-dir

- Source: inline

## Summary

Skip directory traversal because a circular symlink path was already visited.

# Raw Prompt Text
Skipping already visited directory (circular symlink): ${NUM}
