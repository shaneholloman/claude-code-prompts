# System Prompt: refresh-token-session-stale

- Source: inline

## Summary

Handle token refresh for stale session.

# Raw Prompt Text
[${EXPR_1}:token] doRefresh for sessionId=${EXPR_2} stale (gen ${EXPR_3} vs ${EXPR_4}), skipping
