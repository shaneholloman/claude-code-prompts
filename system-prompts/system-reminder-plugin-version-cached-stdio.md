# System Prompt: plugin-version-cached-stdio

- Source: inline

## Summary

Indicates a plugin version is already cached.

# Raw Prompt Text
Plugin ${EXPR_1} version ${EXPR_2} already cached at stdio
