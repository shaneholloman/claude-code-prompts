# System Prompt: duplicate-hooks-file-detected-3

- Source: inline

## Summary

Alert for duplicate hooks file in Claude Desktop.

# Raw Prompt Text
Duplicate hooks file detected: Found ${EXPR_1} MCP ${EXPR_2} in Claude Desktop. resolves to already-loaded file ${NUM}. The standard hooks${PATH} is loaded automatically, so manifest.hooks should only reference additional hook files.
