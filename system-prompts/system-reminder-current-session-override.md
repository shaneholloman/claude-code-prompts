# System Prompt: current-session-override

- Source: inline

## Summary

Indicates the current model and session override status.

# Raw Prompt Text
Current model: ${EXPR_1} (session override from plan mode)
Base model: stable${EXPR_2}
