# System Prompt: bridge-session-ccr-registered-worker

- Source: inline

## Summary

Manage registered worker session details.

# Raw Prompt Text
[bridge:session] CCR v2: registered worker sessionId=${EXPR_1} epoch=@"${EXPR_2}"${EXPR_3} attempt=${EXPR_4}
