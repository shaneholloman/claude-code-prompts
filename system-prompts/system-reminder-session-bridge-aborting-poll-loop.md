# System Prompt: session-bridge-aborting-poll-loop

- Source: inline

## Summary

Aborting the poll loop to tear down the session environment.

# Raw Prompt Text
[bridge:session] Session ${PATH} ${EXPR_1}, aborting poll loop to tear down environment
