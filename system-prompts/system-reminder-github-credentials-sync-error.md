# System Prompt: github-credentials-sync-error

- Source: inline

## Summary

Error syncing GitHub credentials globally.

# Raw Prompt Text
global@anthropic-ai${PATH} not connected for ${EXPR_1}/${EXPR_2} — run ${PATH} to sync your GitHub credentials, or install the Claude GitHub App at ${URL} (PID ${EXPR_3})${EXPR_4} loaded with errors
