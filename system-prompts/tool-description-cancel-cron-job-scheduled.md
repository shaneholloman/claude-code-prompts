# Tool Prompt: cancel-cron-job-scheduled

- Name: CronDelete

## Summary

Cancels a previously scheduled cron job.

# Raw Prompt Text
Cancel a cron job previously scheduled with ${EXPR_1}. Removes it from the in-memory session store.
