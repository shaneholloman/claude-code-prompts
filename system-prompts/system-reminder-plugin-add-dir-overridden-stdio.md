# System Prompt: plugin-add-dir-overridden-stdio

- Source: inline

## Summary

Plugin addition overridden by stdio.

# Raw Prompt Text
Plugin ${EXPR_1} from --add-dir (${EXPR_2}) overridden by ${EXPR_3} (stdio)
