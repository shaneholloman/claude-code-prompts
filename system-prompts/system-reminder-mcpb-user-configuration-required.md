# System Reminder: mcpb-user-configuration-required

- Source: inline

## Summary

MCPB plugin notice that user configuration is required, with menu navigation path.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |
| `EXPR_2` | None | None |

# Raw Prompt Text
MCPB ${EXPR_1} requires user configuration. User can configure via: ${PATH} → Manage plugins → ${EXPR_2} → Configure
