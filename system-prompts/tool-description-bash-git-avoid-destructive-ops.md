# Tool Description: bash-git-avoid-destructive-ops

- Source: native-reference-match

## Summary

Consider safer alternatives before destructive git commands.

# Raw Prompt Text
# Tool Description: bash-git-avoid-destructive-ops

- Source: native-reference-match

## Summary

Before running destructive operations (e.g., git reset --hard, git push --force, git checkout --), consider whether there is a safer alternative that achieve…

# Raw Prompt Text
Before running destructive operations (e.g., git reset --hard, git push --force, git checkout --), consider whether there is a safer alternative that achieves the same goal. Only use destructive operations when they are truly the best approach.
