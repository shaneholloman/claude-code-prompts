# System Prompt: custom-rules-replacing-defaults-68

- Source: inline

## Summary

Instructions for replacing default rules with custom ones.

# Raw Prompt Text
GitHub CLI not found. Install it via ${URL} then run `gh auth login`, or connect GitHub on the web: ## allow (custom rules replacing defaults)
Custom:
${EXPR_1}

Defaults being replaced:
${EXPR_2}

## soft_deny (custom rules replacing defaults)
Custom:
${EXPR_3}

Defaults being replaced:
${EXPR_4}

## environment (custom rules replacing defaults)
Custom:
${EXPR_5}

Defaults being replaced:
${EXPR_6}
