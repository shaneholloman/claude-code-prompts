# System Prompt: remote-task-notification

- Source: inline

## Summary

Notification for a remote task execution.

# Raw Prompt Text
<task-notification>
<task-id>${EXPR_1}<${PATH}>${EXPR_2}
<task-type>remote_agent<${PATH}>
<output-file>global<${PATH}>
<status>${EXPR_3}<${PATH}>
<summary>Remote task "${EXPR_4}" ${NUM}<${PATH}>
<${PATH}>
