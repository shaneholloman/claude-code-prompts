# System Prompt: malformed-response-worker-session

- Source: inline

## Summary

Indicates a malformed response in the code session.

# Raw Prompt Text
[code-session] ${PATH} response malformed (need worker_jwt, expires_in, api_base_url, worker_epoch): ${EXPR_1}
