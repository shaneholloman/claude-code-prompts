# System Prompt: bridge-repl-poll-failures

- Source: inline

## Summary

Exceeded poll failures in bridge repl.

# Raw Prompt Text
[bridge:repl] Poll failures exceeded ${EXPR_1}s (${EXPR_2} errors), giving up
