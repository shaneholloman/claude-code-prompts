# System Prompt: sensitive-content-team-memory

- Source: inline

## Summary

Sensitive content cannot be stored in team memory.

# Raw Prompt Text
Content contains potential secrets (${EXPR_1}, ${EXPR_2}, ${EXPR_3}, ${EXPR_4}, ${EXPR_5}, ${EXPR_6}, ${EXPR_7}, ${EXPR_8}, ## Reserved Shortcuts

${EXPR_9}, ## Available Contexts

${EXPR_10}, ## Available Actions

${EXPR_11}) and cannot be written to team memory. Team memory is shared with all repository collaborators. Remove the sensitive content and try again.
