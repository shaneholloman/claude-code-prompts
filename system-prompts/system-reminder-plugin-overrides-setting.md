# System Prompt: plugin-overrides-setting

- Source: inline

## Summary

One plugin overrides a setting that was previously set by another plugin.

# Raw Prompt Text
Plugin "${EXPR_1}" overrides setting "${EXPR_2}" (previously set by another plugin)
