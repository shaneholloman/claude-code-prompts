# System Reminder: d86fc3e1

- Source: inline

## Summary

HTTP hook must return JSON, but got non-JSON response body: …

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
HTTP hook must return JSON, but got non-JSON response body: ${EXPR_1}
