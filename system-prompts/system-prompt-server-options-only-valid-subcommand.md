# System Prompt: 20e7cc82

- Source: inline

## Summary

SERVER OPTIONS (only valid with the 'server' subcommand) --spawn-same-dir-sessions [<N>] All sessions share the current directory (this is the default; max ……

# Raw Prompt Text
SERVER OPTIONS (only valid with the 'server' subcommand)
  --spawn-same-dir-sessions [<N>]  All sessions share the current directory
                                   (this is the default; max ${NUM} sessions)
  --spawn-worktree-sessions [<N>]  Each session gets an isolated git worktree
                                   (max ${NUM} sessions if N omitted)
