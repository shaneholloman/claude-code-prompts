# System Prompt: bridge-repl-teardown-progress-2

- Source: inline

## Summary

Teardown already in progress, skipping duplicate call.

# Raw Prompt Text
[bridge:repl] Teardown already in progress, skipping duplicate call env=prompt session=${EXPR_1}
