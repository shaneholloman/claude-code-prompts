# System Reminder: save-current-project-config-fallback

- Source: inline

## Summary

Refusing to write due to missing auth in config.

# Raw Prompt Text
saveCurrentProjectConfig fallback: re-read config is missing auth that cache has; refusing to write. See GH #${NUM}.
