# System Prompt: bridge-repl-dropping-pending-messages

- Source: inline

## Summary

Dropping pending messages on transport close.

# Raw Prompt Text
[bridge:repl] Dropping ${EXPR_1} pending message(s) on transport close (code=${EXPR_2})
