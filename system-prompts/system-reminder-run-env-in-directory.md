# System Prompt: run-env-in-directory

- Source: inline

## Summary

Runs a command in a directory with specified environment variables.

# Raw Prompt Text
cd ${EXPR_1} && env ${EXPR_2} ${EXPR_3} ${EXPR_4}@${EXPR_5}${EXPR_6}
