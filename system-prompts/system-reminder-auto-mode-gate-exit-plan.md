# System Prompt: auto-mode-gate-exit-plan

- Source: inline

## Summary

Fallback to default on plan exit due to gate being off.

# Raw Prompt Text
[auto-mode gate @ ExitPlanModeV2Tool] prePlanMode=global but gate is off (reason=${EXPR_1}) — falling back to default on plan exit
