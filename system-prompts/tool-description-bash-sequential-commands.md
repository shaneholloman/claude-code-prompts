# Tool Description: bash-sequential-commands

- Source: native-reference-match

## Summary

If the commands depend on each other and must run sequentially, use a single … call with '&&' to chain them together.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
If the commands depend on each other and must run sequentially, use a single ${EXPR_1} call with '&&' to chain them together.
