# System Reminder: git-repo-url-specify

- Source: inline

## Summary

Specify a repo URL manually if not in a git repo.

# Raw Prompt Text
Not in a git repo — you'll need to specify a repo URL manually (or skip repos entirely).
