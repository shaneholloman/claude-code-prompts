# System Reminder: save-config-lock-auth-missing

- Source: inline

## Summary

Refusing to write due to missing auth in config.

# Raw Prompt Text
saveConfigWithLock: re-read config is missing auth that cache has; refusing to write to avoid wiping ~${PATH} See GH #${NUM}.
