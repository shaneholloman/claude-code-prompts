# System Prompt: auto-gate-exit-plan-fallback-2

- Source: inline

## Summary

Fallback to default plan due to gate errors.

# Raw Prompt Text
[auto-mode gate @ ExitPlanModeV2Tool] prePlanMode=${EXPR_1} but gate is off (reason=${EXPR_2} loaded with errors) — falling back to default on plan exit
