# System Reminder: 64135320

- Source: inline

## Summary

HTTP hook must return valid JSON, but parsing failed: …

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
HTTP hook must return valid JSON, but parsing failed: ${EXPR_1}
