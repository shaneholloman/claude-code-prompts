# System Prompt: do-refresh-session-errors

- Source: inline

## Summary

Handle token refresh with session errors.

# Raw Prompt Text
[${EXPR_1}:token] doRefresh for sessionId=${EXPR_2} loaded with errors stale (gen ${EXPR_3} vs ${EXPR_4}), skipping
