# System Prompt: bridge-repl-init-bridge-core

- Source: inline

## Summary

Initialize the bridge core with starting messages.

# Raw Prompt Text
[bridge:repl] initBridgeCore #${EXPR_1} starting (initialMessages=${EXPR_2} perpetual prior=env:${EXPR_3})
