# System Prompt: html-progress-bar-row

- Source: inline

## Summary

HTML snippet template for rendering a labeled progress bar row with fill width and color.

# Raw Prompt Text
<div class="bar-row">
        <div class="bar-label">${EXPR_1}<${PATH}>
        <div class="bar-track"><div class="bar-fill" style="width:${EXPR_2}%;background:${EXPR_3}"><${PATH}><${PATH}>
        <div class="bar-value">local<${PATH}>
      <${PATH}>
