# System Prompt: team-memory-sync-warning

- Source: inline

## Summary

Local entries exceed server capacity limit.

# Raw Prompt Text
team-memory-sync: ${EXPR_1} local entries exceeds server cap of ${EXPR_2}; ${EXPR_3} file(s) will NOT sync: ${EXPR_4}, ${EXPR_5}, ${EXPR_6}, ${EXPR_7}, ${EXPR_8}. Consider consolidating or removing some team memory files.
