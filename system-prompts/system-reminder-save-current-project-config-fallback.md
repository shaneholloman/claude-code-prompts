# System Reminder: f72b8c4d

- Source: inline

## Summary

saveCurrentProjectConfig fallback: re-read config is missing auth that cache has; refusing to write.

# Raw Prompt Text
saveCurrentProjectConfig fallback: re-read config is missing auth that cache has; refusing to write. See GH #${NUM}.
