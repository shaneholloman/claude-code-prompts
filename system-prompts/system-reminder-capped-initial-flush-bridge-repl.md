# System Prompt: capped-initial-flush-bridge-repl

- Source: inline

## Summary

Handles capped initial flush in bridge repl.

# Raw Prompt Text
[bridge:repl] Capped initial flush: ${EXPR_1} -> ${EXPR_2} (cap=${EXPR_3})
