# System Prompt: cleanup-skipped-corrupted-path

- Source: inline

## Summary

Cleanup skipped due to corrupted path.

# Raw Prompt Text
Skipping cleanup of old installLocation (${EXPR_1}) — outside ${EXPR_2}. The path is corrupted; leaving it alone and overwriting the config entry.
