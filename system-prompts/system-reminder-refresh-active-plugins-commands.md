# System Prompt: refresh-active-plugins-commands

- Source: inline

## Summary

Refresh active plugins with specified commands and agents.

# Raw Prompt Text
refreshActivePlugins: ${EXPR_1} enabled, ${EXPR_2} commands, ${EXPR_3} agents, ${EXPR_4} hooks, @anthropic-ai${PATH} MCP,  (PID ${EXPR_5}) LSP
