# System Prompt: bridge-repl-initial-flush-dropped

- Source: inline

## Summary

Initial flush dropped batches without marking UUIDs.

# Raw Prompt Text
[bridge:repl] Initial flush dropped ${EXPR_1} batch(es) — not marking ${EXPR_2} UUID(s) as flushed
