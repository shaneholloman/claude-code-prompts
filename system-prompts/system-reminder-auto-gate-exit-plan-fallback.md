# System Prompt: auto-gate-exit-plan-fallback

- Source: inline

## Summary

Fallback to default plan exit when gate is off.

# Raw Prompt Text
[auto-mode gate @ ExitPlanModeV2Tool] prePlanMode= (PID ${EXPR_1}) but gate is off (reason=${EXPR_2}) — falling back to default on plan exit
