# System Reminder: validate-session-uuid

- Source: inline

## Summary

Reject a non-UUID session ID and echo the invalid provided value.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
. Session IDs must be in UUID format (e.g., ${UUID}). Provided value "${EXPR_1}" is not a valid UUID
