# System Prompt: autonomous-default-dynamic-pacing

- Source: inline

## Summary

Run autonomous checks with dynamic pacing.

# Raw Prompt Text
# ${PATH} — autonomous default with dynamic pacing

The user invoked `${PATH}` with no prompt and no interval. Run the autonomous check now, then self-pace the next iteration via ScheduleWakeup — no cron.
