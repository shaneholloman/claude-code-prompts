# System Prompt: loaded-inline-content-command

- Source: inline

## Summary

Reports an inline content command loaded from a plugin with its PID.

# Raw Prompt Text
Loaded inline content command from plugin ${EXPR_1}:  (PID ${EXPR_2})
