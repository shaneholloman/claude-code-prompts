# System Prompt: ripgrep-error-with-results

- Source: inline

## Summary

Reports a ripgrep error with signal, code, stderr, and result count.

# Raw Prompt Text
rg error (signal=${EXPR_1}, code=${EXPR_2}, stderr: ${EXPR_3}), ${EXPR_4} results
