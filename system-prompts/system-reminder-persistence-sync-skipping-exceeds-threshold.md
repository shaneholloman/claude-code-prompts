# System Prompt: persistence-sync-skipping-exceeds-threshold

- Source: inline

## Summary

Skipping due to bytes exceeding the threshold.

# Raw Prompt Text
[persistence-sync] Skipping ${EXPR_1} — ${EXPR_2} bytes exceeds ${NUM} threshold
