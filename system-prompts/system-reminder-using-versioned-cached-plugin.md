# System Prompt: using-versioned-cached-plugin

- Source: inline

## Summary

Utilizing a versioned cached plugin ZIP from a specific PID.

# Raw Prompt Text
Using versioned cached plugin ZIP ${EXPR_1} from  (PID ${EXPR_2})
