# Tool Description: bash-sandbox-no-sensitive-paths

- Source: native-reference-match

## Summary

Do not suggest adding sensitive paths like ~…, ~…, ~…*, or credential files to the sandbox allowlist.

# Raw Prompt Text
Do not suggest adding sensitive paths like ~${PATH}, ~${PATH}, ~${PATH}*, or credential files to the sandbox allowlist.
