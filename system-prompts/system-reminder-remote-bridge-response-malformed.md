# System Prompt: remote-bridge-response-malformed

- Source: inline

## Summary

Indicates a malformed response requiring specific parameters.

# Raw Prompt Text
[remote-bridge] ${PATH} response malformed (need worker_jwt, expires_in, api_base_url, worker_epoch): ${EXPR_1}
