# System Prompt: team-memory-sync-conflict-probing

- Source: inline

## Summary

Handling team memory sync conflicts with probing.

# Raw Prompt Text
team-memory-sync: conflict (${NUM}), probing server hashes (attempt ${EXPR_1}${NUM}/${NUM})
