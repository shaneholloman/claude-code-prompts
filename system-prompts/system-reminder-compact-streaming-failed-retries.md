# System Reminder: compact-streaming-failed-retries

- Source: inline

## Summary

Compact streaming failed after repeated attempts, reporting hasStartedStreaming state and PID.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |
| `EXPR_2` | None | None |

# Raw Prompt Text
Compact streaming failed after ${EXPR_1} attempts. hasStartedStreaming= (PID ${EXPR_2})
