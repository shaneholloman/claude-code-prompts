# System Reminder: cannot-acquire-lock-held-by-pid

- Source: inline

## Summary

Indicates a lock cannot be acquired because another process PID holds it.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |
| `EXPR_2` | None | None |

# Raw Prompt Text
Cannot acquire lock for ${EXPR_1} - held by PID ${EXPR_2}
