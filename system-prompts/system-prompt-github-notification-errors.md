# System Prompt: github-notification-errors

- Source: inline

## Summary

Notification for loaded task errors in GitHub.

# Raw Prompt Text
<task-notification>
<task-id>${EXPR_1}<${PATH}>${EXPR_2} loaded with errors
<output-file> (PID ${EXPR_3})<${PATH}>
<status>${EXPR_4}<${PATH}>
<summary>${EXPR_5}<${PATH}>${EXPR_6}waiting_for_agentsGitHub not connected for ${EXPR_7}/${EXPR_8} — run ${PATH} to sync your GitHub credentials, or install the Claude GitHub App at ${URL}
<${PATH}>
