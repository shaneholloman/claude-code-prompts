# System Prompt: github-npm-version-check

- Source: inline

## Summary

Check stable npm version and GitHub connection.

# Raw Prompt Text
stable

npm view ${EXPR_1}@${EXPR_2} version

local

${EXPR_3}

${EXPR_4} loaded with errors

GitHub not connected for ${EXPR_5}/${EXPR_6} — run ${PATH} to sync your GitHub credentials, or install the Claude GitHub App at ${URL}
