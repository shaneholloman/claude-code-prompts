# System Prompt: refresh-token-session-expiry

- Source: inline

## Summary

Refresh token if session expiry is imminent.

# Raw Prompt Text
[${EXPR_1}:token] Token for sessionId=${EXPR_2} expires=${EXPR_3} (past or within buffer), refreshing immediately
