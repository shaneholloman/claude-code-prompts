# System Prompt: schedule-autonomous-default

- Source: inline

## Summary

Schedules the autonomous default and runs the first check.

# Raw Prompt Text
# ${PATH} — schedule the autonomous default

The user invoked `${PATH}` with no prompt (input was empty or just the interval `local`). Schedule the autonomous-loop default and then run the first autonomous check immediately.
