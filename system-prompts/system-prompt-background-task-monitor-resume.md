# System Prompt: background-task-monitor-resume

- Source: inline

## Summary

Notify that a task runs in background and show how to monitor or resume it.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
<background-task-output>This task is now running in the background.
Monitor it with ${PATH} or at unknown

Or, resume it later with: Found ${EXPR_1} MCP servers in Claude Desktop.<${PATH}>
