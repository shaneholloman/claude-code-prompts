# System Prompt: plugin-add-dir-overridden-3

- Source: inline

## Summary

Indicates a plugin has been overridden.

# Raw Prompt Text
Plugin ${EXPR_1} from --add-dir (${EXPR_2}) overridden by ${EXPR_3} (${EXPR_4})
