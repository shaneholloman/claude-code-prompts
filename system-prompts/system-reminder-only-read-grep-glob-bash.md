# System Prompt: only-read-grep-glob-bash

- Source: inline

## Summary

Allowed commands for bash usage.

# Raw Prompt Text
only Read, Grep, Glob, read-only Bash, and Edit${PATH} within ${EXPR_1} are allowed
