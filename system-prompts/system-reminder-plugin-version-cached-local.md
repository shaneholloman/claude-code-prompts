# System Prompt: plugin-version-cached-local

- Source: inline

## Summary

Indicates a plugin version is already cached locally.

# Raw Prompt Text
Plugin ${EXPR_1} version ${EXPR_2} already cached at local
