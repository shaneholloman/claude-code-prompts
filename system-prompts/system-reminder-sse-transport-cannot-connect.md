# System Prompt: sse-transport-cannot-connect

- Source: inline

## Summary

Reject an SSETransport connection attempt because the transport is in a given state.

# Raw Prompt Text
SSETransport: Cannot connect, current state is ${EXPR_1}
