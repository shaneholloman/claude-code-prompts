# System Reminder: skip-cleanup-invalid-settings

- Source: inline

## Summary

Cleanup skipped because settings validation errors persist.

# Raw Prompt Text
Skipping cleanup: settings have validation errors but cleanupPeriodDays was explicitly set. Fix settings errors to enable cleanup.
