# System Prompt: remote-bridge-capped-initial-flush

- Source: inline

## Summary

Capped initial flush for remote bridge configuration.

# Raw Prompt Text
[remote-bridge] Capped initial flush: ${EXPR_1} -> ${EXPR_2} (cap=${EXPR_3})
