# System Prompt: autonomous-cron-scheduled-tasks

- Source: inline

## Summary

Multiple prompts (2)

# Raw Prompt Text
${EXPR_1}what's scheduled, the cron expression, the human-readable cadence, that recurring tasks auto-expire after ${EXPR_2} days, and that they can cancel sooner with ${EXPR_3} (include the job ID). Mention this is the autonomous default and that the autonomous-loop instructions are baked in.
