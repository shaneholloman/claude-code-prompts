# System Prompt: bridge-repl-teardown-perpetual-2

- Source: inline

## Summary

Leaving session alive on server for a duration.

# Raw Prompt Text
[bridge:repl] Teardown (perpetual): leaving env=prompt session=${EXPR_1} alive on server, duration=${EXPR_2}ms
