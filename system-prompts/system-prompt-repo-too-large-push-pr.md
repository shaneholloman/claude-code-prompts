# System Prompt: repo-too-large-push-pr

## Summary

Notify user to push a PR instead of direct repo push.

# Raw Prompt Text
Repo is too large. Push a PR and use `${PATH} <PR#>` instead. Tell the user this.
