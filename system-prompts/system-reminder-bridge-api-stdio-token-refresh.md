# System Prompt: bridge-api-stdio-token-refresh

- Source: inline

## Summary

Attempting token refresh for received stdio data.

# Raw Prompt Text
[bridge:api] stdio: ${NUM} received, attempting token refresh
