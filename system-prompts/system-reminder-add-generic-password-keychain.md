# System Prompt: add-generic-password-keychain

- Source: inline

## Summary

Add or update a generic password in keychain for an account and service.

# Raw Prompt Text
add-generic-password -U -a "${EXPR_1}" -s "${EXPR_2}" -X "${NUM}"
