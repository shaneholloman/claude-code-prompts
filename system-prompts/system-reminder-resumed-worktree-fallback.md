# System Reminder: resumed-worktree-fallback

- Source: inline

## Summary

Fallback to parent cwd due to missing worktree.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
Resumed worktree ${EXPR_1} no longer exists; falling back to parent cwd
