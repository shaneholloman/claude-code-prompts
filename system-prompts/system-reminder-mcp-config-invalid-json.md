# System Prompt: mcp-config-invalid-json

- Source: inline

## Summary

Invalid JSON detected in MCP config.

# Raw Prompt Text
MCP config is not valid JSON: ${EXPR_1} (scope=${EXPR_2}, length=${EXPR_3}, first100=${EXPR_4})
