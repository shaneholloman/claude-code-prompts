# System Prompt: invalid-shell-path-fallback

- Source: inline

## Summary

Warns configured shell path is not valid and falls back to auto-detection.

# Raw Prompt Text
CLAUDE_CODE_SHELL="${EXPR_1}" is not a valid bash${PATH} path, falling back to detection
