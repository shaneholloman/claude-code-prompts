# System Prompt: bash-permissions-subcommands-exceeds-cap

- Source: inline

## Summary

Handling bash permissions when subcommands exceed limits.

# Raw Prompt Text
bashPermissions: ${EXPR_1} subcommands exceeds cap (${NUM}) — returning ask
