# System Prompt: found-mcp-desktop-2

- Source: inline

## Summary

Found MCP in Claude Desktop environment.

# Raw Prompt Text
cd ${EXPR_1} && env ${EXPR_2} ${EXPR_3} Found ${EXPR_4} MCP ${EXPR_5} in Claude Desktop.${NUM}
