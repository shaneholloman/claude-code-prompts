# System Prompt: plugin-add-dir-overridden-2

- Source: inline

## Summary

Notes a plugin was overridden by a global plugin.

# Raw Prompt Text
Plugin ${EXPR_1} from --add-dir (${EXPR_2}) overridden by ${EXPR_3} (local)
