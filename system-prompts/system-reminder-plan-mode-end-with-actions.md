# System Prompt: plan-mode-end-with-actions

- Source: inline

## Summary

Requires ending each turn with a question or plan-approval action in plan mode.

# Raw Prompt Text
Plan mode still active (see full instructions earlier in conversation). Read-only except plan file (${EXPR_1}). ${EXPR_2} End turns with AskUserQuestion (for clarifications) or ${EXPR_3} (for plan approval). Never ask about plan approval via text or AskUserQuestion.
