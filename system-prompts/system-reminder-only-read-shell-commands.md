# System Prompt: only-read-shell-commands

- Source: inline

## Summary

Read-only shell commands are allowed.

# Raw Prompt Text
Only read-only shell commands and rm with all paths inside ${EXPR_1} are permitted in this context (ls, find, grep, cat, stat, wc, head, tail, and similar)
