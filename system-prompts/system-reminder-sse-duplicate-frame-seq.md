# System Prompt: sse-duplicate-frame-seq

- Source: inline

## Summary

Logs duplicate SSE frame sequence for a stream, including last sequence and count.

# Raw Prompt Text
SSETransport: DUPLICATE frame seq=@anthropic-ai${PATH} (lastSequenceNum=${EXPR_1}, seenCount=${EXPR_2})
