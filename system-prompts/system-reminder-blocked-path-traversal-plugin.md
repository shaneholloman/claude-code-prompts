# System Prompt: blocked-path-traversal-plugin

- Source: inline

## Summary

Warns that a plugin path traversal attempt was blocked, with plugin and details.

# Raw Prompt Text
Security: Path traversal attempt blocked in plugin ${EXPR_1}: ${EXPR_2}
